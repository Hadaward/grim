"use client";

import { makeStyle } from "@/components/util/style";

export default makeStyle({
    world: {
        width: 1024,
        height: 576,
        backgroundRepeat: "no-repeat",
        position: "relative"
    }
});