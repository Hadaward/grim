"use client";

import { makeStyle } from "@/components/util/style";

export default makeStyle({
    world: {
        width: "100%",
        height: "100%",
        backgroundRepeat: "no-repeat",
        position: "relative"
    }
});